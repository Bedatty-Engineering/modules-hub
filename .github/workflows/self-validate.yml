name: Validate

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

permissions:
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/validate.yml
    with:
      is_pull_request: ${{ github.event_name == 'pull_request' }}
